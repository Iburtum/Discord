# Discord Bot Token — from https://discord.com/developers/applications
DISCORD_TOKEN=
# Discord Application (Client) ID
CLIENT_ID=
# GitHub fine-grained PAT with Models (read) permission — used for AI inference via GitHub Models
# Do NOT name this GITHUB_TOKEN: that name is reserved by GitHub Actions/Codespaces and would collide.
GH_MODELS_TOKEN=
